"""Integration test tier: exercises REAL git, tmux, and `backlog` CLI
processes in fully sandboxed environments. See README.md in this
directory for the sandboxing contract, and run explicitly with:

    python3 -m unittest discover tests_integration

Never run as part of the default `python3 -m unittest discover tests`
suite.
"""
